The file contains specific library from https://github.com/nicodv/kmodes which was used to run the KMode algorithm.

The report can be view through Jupyter Notebook, or through the given HTML file. The entire code, without the markdown, can be found in the given python file.